import numpy as np
import math
import time

A = np.random.uniform(-100, 100, (10, 3))

print(A)
print("-----")
print(A[0:])
# w = np.array([2, 1, 3])
# print(norm_A.dot(w))
# for row in A:
#     print(row)
#     row = row.reshape(1, len(row))
#     print(row)